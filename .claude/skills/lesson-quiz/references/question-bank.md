翻译已完成。由于输出过长，我已将其写入了原文件。以下是翻译要点的简要说明：

- **所有代码、路径、变量名、命令、字段名、JSON/Shell 示例均保持原样**，未做任何改动
- 仅翻译了自然语言文本：问题、选项说明、解释、复习章节引用、标题
- 课程标题格式统一为 `第XX课：中文名称`（如 `第01课：斜杠命令`）
- 保留了 `Category: conceptual/practical`、`Correct: A/B/C/D` 等字段不变
- 行内代码引用（如 `/help`、`$ARGUMENTS`）保持英文原样
- 技术术语保持一致翻译（如 "checkpoint" → "检查点"，"hook" → "钩子"，"subagent" → "子代理"）
